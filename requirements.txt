# ============================================================
# ReFine-Lab: Requirements (pip fallback)
# Prefer: pip install -e ".[all]" (uses pyproject.toml)
# ============================================================

# Core ML
torch>=2.1.0
transformers>=4.38.0
datasets>=2.16.0
tokenizers>=0.15.0
accelerate>=0.27.0

# Fine-tuning & RL
peft>=0.8.0
bitsandbytes>=0.42.0
trl>=0.14.0

# Experiment tracking
wandb>=0.16.0

# Evaluation
evaluate>=0.4.0
scikit-learn>=1.3.0

# Data
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.66.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.13.0

# NOTE: Install separately on GPU machines:
#   pip install flash-attn --no-build-isolation
#   pip install auto-gptq  (for quantization)

